// src/roulette/roulette-room.service.ts
import { Injectable, Logger } from '@nestjs/common';

@Injectable()
export class RouletteRoomService {
  private readonly logger = new Logger(RouletteRoomService.name);

  constructor() {}

  // Aqui iremos implementar os métodos para gerenciar as salas de roleta online
}